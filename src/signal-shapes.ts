/** Public schema and source-note coordinates shared by the project scenes. */
export interface Note { start: number; duration: number; pitch: number }
export const schemaFields = ['acl_tear', 'mcl_injury', 'meniscus_tear', 'fracture', 'osteoarthritis', 'effusion'];
